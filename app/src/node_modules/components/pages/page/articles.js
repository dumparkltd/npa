var React           = require('react')
var Fluxxor         = require('fluxxor')
var StoreWatchMixin = Fluxxor.StoreWatchMixin

//components
var EntityTable            = require('components/list/EntityTable')


//helpers

var _               = require('lodash')

var log       = require('debug')('src:components:pages:articles')

module.exports = React.createClass({
  mixins: [
    Fluxxor.FluxMixin(React),
    StoreWatchMixin('articles')
  ],

  getStateFromFlux: function () {
    var flux = this.getFlux()        
    return {
      termsLoading: flux.store('articles').isLoading(),
      
    }
  },

  render: function () {
//    log('props, state', this.props, this.state)
    var flux = this.getFlux()        
    if (this.state.pagesLoading ) {
      return  (
        <Load />
      )
    } else {
      var entities = flux.store('articles').entities()
      var cols = [
                {type:'text', title: 'Nr.',      attr:'id'},
                {type:'text', title: 'English',  attr:'title'},
                {type:'text', title: 'Te reo Maori',  attr:'titlemaori'}
        ]
      return (
          <EntityTable
            cols = {cols}
            entities = {entities} />
      )      
    }
    
  }
 

})
