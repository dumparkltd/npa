var React           = require('react')
var Fluxxor         = require('fluxxor')
var StoreWatchMixin = Fluxxor.StoreWatchMixin

//components

// bootstrap
var Row 				= require('react-bootstrap/lib/Row')
var Col 				= require('react-bootstrap/lib/Col')

//helpers

var _               = require('lodash')

var log       = require('debug')('src:components:subjects:recommendation')

module.exports = React.createClass({
  mixins: [
    Fluxxor.FluxMixin(React),
    StoreWatchMixin('actions','recommendations','issues','groups','treatybodies')
  ],

  getStateFromFlux: function () {
    var flux = this.props.flux
    return {
      actionsLoading : flux.store("actions").isLoading(),
      entitiesLoading : flux.store("recommendations").isLoading(),
      issuesLoading: flux.store('issues').isLoading(),
      groupsLoading : flux.store("groups").isLoading(),
      treatybodiesLoading : flux.store("treatybodies").isLoading()      
    }
  },

  render: function () {
    var flux = this.props.flux

    return (
      <Row>
        { this.renderMain() }
      </Row>
    )
  },
  renderMain : function (){
    var flux = this.props.flux
    
    var id = 
    
    var entity = flux.stores.recommendations.findOne({ id: this.props.params.id })
log('recommendation',entity)
    return (
      <Col md={12}>
        <p>Recommendation </p>
        <p>
          { (this.state.entitiesLoading) ? 'loading' : entity.title }
        </p>
      </Col>
    )
  },
 
 

})
