var moment = require('moment')
module.exports = function (date) {
  return date.format() === 'Invalid date' 
    ? '' 
    : date._f === 'YYYY'
    ? date.format('YYYY')
      : date._f === 'MMM-YY' || date._f === 'MM/YYYY'
      ? date.format('MMMM YYYY')
        : date.format('D MMMM YYYY')
}